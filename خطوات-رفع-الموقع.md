# تشغيل موقع "حكايتنا" لكل عميل (زيرو تعب على العميل)

العميل **مش محتاج حساب GitHub ولا يعمل أي حاجة** — إنت بتعمل الريبو وتنشر وتتأكد إن كل شىء شغال، وتسلم للولد **لينك + كلتتين** بس.

## إزاي النظام شغال

- الموقع شغال على لينك ثابت.
- الولد بيكتب كلمته → يفتح له زرار **⚙** (لوحة التعديل) → يعدل أي حاجة في أي وقت → يضغط **"احفظ وانشر 💾"**.
- التعديلات بتتكتب في ملف `data.json` في الريبو، فأول ما البنت تفتح نفس اللينك تشوف آخر نسخة (ولو مفتوحاه بتتحدث تلقائيًا كل ثواني).
- مفتاح التعديل (Token) بيتحفظ في متصفح الولد بس — **مش بيتكتب في الموقع ولا في الكود**.

---

## التسليم لكل عميل جديد (الخطوات المطلوبة منك مرة واحدة لكل عميل)

### 1) جِهّز ملفات العميل من اللوحة

1. افتح `builder.html`.
2. املأ بيانات العميل (الأسماء، الكلمة السرية للبنت، كلمة الولد، القصة، الأغنية…).
3. دمّل الفعل:
   - زر **"تحميل موقع العميل"** → احفظه باسم **`index.html`** (مهم جدًا عشان اللينك يبقى قصير).
   - زر **"تنزيل data.json"** → احفظه جنبه.
   - **عشان التعديلات تظهر صح من أول يوم لازم تنزّل الاتنين مع بعض.**

### 2) اعمل ريبو للعميل على حسابك

1. GitHub → **New repository**.
2. الاسم: حاجة مخصوصة للعميل، زي `story-ahmed-sara`.
3. الاختيار **Public** (جبرى — عشان GitHub Pages يشتغل مجانًا وملف البيانات يتوصل) — **خلي الريبو Public دايمًا**.
4. **Create repository**.

### 3) ارفع الملفين

- **Add file → Upload files** → سحب `index.html` و `data.json` → **Commit changes**.

### 4) فعّل النشر (Pages)

- **Settings → Pages → Branch: `main` / root → Save**.
- استنى 1–2 دقيقة، واللينك بيبقى:
  **`https://[اسمك].github.io/[اسم-الريبو]/`**
  افتحه تتأكد إن شاشة القفل ظهرت.

### 5) اعمل مفتاح مخصوص للعميل ده بس (شغله من النفاح)

السر هنا إن كل عميل ياخد **Token مخصوص بريبوه هو بس** — لو اتسرّب، بيصلّح ريبو واحد بس، وعادي تلغيه وتغيّره.

1. GitHub → **Settings** (الصورة بتاعتك) → **Developer settings** → **Personal access tokens** → **Fine-grained tokens** → **Generate new token**.
2. **Resource owner**: اسمك. **Repository access**: اختيار **Only select repositories** واختار ريبو العميل ده.
3. من **Permissions** → **Repository permissions → Contents**: اختار **Read and write**.
4. **Generate token** → **انسخه فورًا** (بيظهر مرة واحدة).

### 6) سلم الولد "لينك التثبيت" مرة واحدة

بعد اللي فوق، ابعت للولد لينك زي ده:

```
https://اسمك.github.io/اسم-الريبو/?t=نسخه-الهي-ال-TOKEN
```

- أول ما الولد يفتحه: بيظهر له تنبيه "تم ربط مفتاح التعديل ✅" والمفتاح بيتحفظ في متصفحه واللينك بيتنضّف أوتوماتيك.
- الباسوردات الافتراضية اللي اتسلّمو بيها: **البنت تكتب `بحبك`** و **الولد يكتب `هيرو`** (يقدروا يغيّروهم من ⚙).
- من بعدها يفتح اللينك **النضيف بس**: يكتب كلمته → ⚙ → يعدّل → "احفظ وانشر".
- **حذّر العميل**: لينك التثبيت ده (اللي جواه الـ token) مخصوص له هو، مترسلوش لأي حد.

---

## من هنا بعد كده

- الولد يعدّل في أي وقت والبيانات بتظهر للبنت على نفس اللينك.
- عشان يغيّر الباسوردين: من **⚙** → شاشة الدخول → يعيد كتابة كلتة البنت وكلتته → **احفظ وانشر**.
- لو عميل طلب صور حقيقية: ارفع صوره في نفس الريبو وخد روابطها (أو رابط خارجي).

---

## تذكيرات سريعة

- **الريبو لازم Public** - لو خصوصي هيتخير برو المدفوع ومش هيشغّل.
- التسليم دايمًا **`index.html` + `data.json` مع بعض** وباسم `index.html`.
- كل عميل له **Token منفصل** على ريبوه بس — متعبّتش كل الريبوهات بـ token واحد عام.
- **الولد ميشوفش التوكن ولا كلمة GitHub قدامه إطلاقًا** — بيتربط مرة واحدة باللينك السحري، وبعدها يعدّل بس.
- **الصور اللي الولد يرفعها من موبايله/كمبيوتره** بتتحفظ أوتوماتيك في فولدر `uploads` في ريبو العميل (بيتم تصغيرها وضغطها قبل الرفع). لأن الريبو Public، أي حد عند اللينك يقدر يشوف الصور — متوقع ومناسب للطبيعة دي.
- لو الـ token اتحرق: اعمل Token جديد من نفس المكان، ابعت للولد لينك تثبيت جديد (بنفس الخاصية `?t=`)، والتعديلات بتكمل طبيعي.
- جرب بنفسك مرة كام: افتح اللينك، اكتب كلمة الولد، عدّل حاجة، احفظ، اعيد فتح اللينك تتأكد إن البنت هتشوفها.